export default function DashboardButton({ buttonText }) {
  return (
    <button className=" bg-white dark:bg-[#444] text-light_blue text-l px-4 py-2 rounded">
      {buttonText}
    </button>
  );
}
