'use client';

import { TypeAnimation } from 'react-type-animation';

const ClientTypeAnimation = (props: React.ComponentProps<typeof TypeAnimation>) => {
  return <TypeAnimation {...props} />;
};

export default ClientTypeAnimation; 